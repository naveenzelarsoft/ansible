- name: Install dependencies
  apt:
    name: npm
    update_cache: yes
    state: present

- name: Download frontend from Nexus
  get_url:
    url: http://3.238.190.59:8081/repository/todo/todo-0.0.3.zip
    dest: /root/todo.zip
    username: admin
    password: admin

- name: Unzip the Downloaded Content
  unarchive:
    src: /root/todo.zip
    dest: /root
    remote_src: yes

- name: Copy todos Service file
  template:
    src: todo.service
    dest: /etc/systemd/system/todo.service

- name: Start {{COMPONENT}} Service
  systemd:
    name: todo
    daemon_reload: yes
    state: started
    enabled: yes